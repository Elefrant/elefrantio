#!/usr/bin/env node

'use strict';

var cli = require('../lib/cli'),
  program = require('commander');

program
  .description('Registers a user on the elefrant.com network.')
  .parse(process.argv);

cli.register();
